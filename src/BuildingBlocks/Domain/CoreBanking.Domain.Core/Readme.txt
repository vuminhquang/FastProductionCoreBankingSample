Services here are just interface for Infra to implement.

Basically, Services interfaces and Commands/Queries are the same, as they are only the abstract, which Infra should do.
But we use services interfaces for classes with many functions, while Commands/Queries only handle 1 command only. 